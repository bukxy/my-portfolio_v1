# Dev uniquement – hot-reload avec Spring Boot DevTools
FROM maven:3.9-eclipse-temurin-21

WORKDIR /app

# Force la connexion MongoDB vers le conteneur mongo
ENV SPRING_DATA_MONGODB_URI=mongodb://mongo:27017/portfolio
ENV SPRING_PROFILES_ACTIVE=dev

# Cache des dépendances Maven
COPY back/pom.xml .
RUN mvn dependency:go-offline -q

# Le code source est monté via volume dans docker-compose.dev.yml
EXPOSE 8080

CMD ["mvn", "spring-boot:run"]
